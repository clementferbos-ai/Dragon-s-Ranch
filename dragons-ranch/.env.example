# Variables d'environnement Turso — à configurer dans Vercel
# (Project Settings > Environment Variables), JAMAIS commitées.
#
# Pour tester en local avec `vercel dev`, copier ce fichier en `.env.local`
# et y mettre les vraies valeurs.

TURSO_DATABASE_URL=libsql://ta-base.turso.io
TURSO_AUTH_TOKEN=ton-token-secret
